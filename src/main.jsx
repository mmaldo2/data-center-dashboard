import React from "react";
import ReactDOM from "react-dom/client";
import FusedDashboard from "../Data center dashboard.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FusedDashboard />
  </React.StrictMode>
);
